name: macOS

on:
  push:
    branches:
      - master

jobs:
  ci:
    runs-on: macos-latest
    steps:
      - run: curl -o- https://raw.githubusercontent.com/GodD6366/dotfiles/main/_install/macos.zsh | zsh
